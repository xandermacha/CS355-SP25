#include <stdio.h>

int main() {
    char cLetter = 'A'; // ! hungarian notation char uses ''
    int iNumber = 0;
    float fStuff = 10.1111;

    printf("the value of cLetter is %d, which is %c\n", cLetter, cLetter);
    return iNumber;
}